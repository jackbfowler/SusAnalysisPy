"""Suspension geometry analyzer (shock/bump travel only, no steering).

Port of ``SusAnalysis/SussyAnal/sussy_shock_only.m``.

Parses geometry and configuration from a CSV, then runs the non-iterative
geometric solver over shock travel (bump/droop).
"""
